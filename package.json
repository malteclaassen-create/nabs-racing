{
  "name": "nabs-racing",
  "private": true,
  "description": "NABS Racing League - one deployable unit: the backend serves the built frontend, the API and the downloads (see DEPLOYMENT.md)",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix backend install",
    "start": "npm --prefix backend run start:prod",
    "test": "npm --prefix backend test && node --test \"frontend/src/**/*.test.mjs\""
  },
  "engines": {
    "node": ">=20"
  }
}
